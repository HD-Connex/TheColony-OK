{
  "name": "get_domain_keyword_suggestions",
  "description": "Returns the organic keywords a domain ranks for, including position and available metrics. Use after get_domain_overview when you want the detailed keyword opportunity list for a competitor or reference domain. Charges credits (~100-300 typical). Cached for 12 hours.",
  "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "projectId": {
        "type": "string",
        "minLength": 1,
        "description": "Required. The OpenSEO project ID to scope this call to. Get one from list_projects."
      },
      "domain": {
        "type": "string",
        "minLength": 1,
        "description": "Competitor or reference domain to extract keywords from."
      },
      "locationCode": {
        "type": "integer",
        "exclusiveMinimum": 0,
        "maximum": 9007199254740991,
        "description": "DataForSEO location code. Defaults to 2840 (United States). See dataforseo.com/help-center/locations."
      },
      "languageCode": {
        "type": "string",
        "minLength": 2,
        "description": "Language code (e.g. 'en', 'es', 'fr'). Defaults to 'en'."
      }
    },
    "required": [
      "projectId",
      "domain"
    ]
  }
}