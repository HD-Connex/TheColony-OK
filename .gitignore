.env
.env.local
.env.*.local
.env.vercel.test
.env.vercel.*.tmp
node_modules/
.next/
.vercel/
terminals/
*.tsbuildinfo

.vercel
.env*
# ...but always allow committing example env templates (no real secrets)
!.env.example
!**/.env.example

# P2-17 repo hygiene: future *.log (build-*, test-*, rate-*, npm-*, tsc-*) go to _archived_ or ignored.
# Old ones cleaned; active PHASE* md kept in root for launch notes.
*.log
!PHASE5*.md
!PHASE*.log

.claude/
.serena/
.config/
bun.lock

# local agent skills / MCP config (tooling-specific, not project source)
.agents/
.mcp.json
test-results/

# nested repo
thecolony-app/
